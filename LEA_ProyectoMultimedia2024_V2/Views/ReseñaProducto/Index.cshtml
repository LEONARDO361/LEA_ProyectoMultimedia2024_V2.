@model IEnumerable<LEA_ProyectoMultimedia2024_V2_.Models.Tables.ReseñaProducto>

@{
    ViewData["Title"] = "Index";
}

<h1>Reseñas </h1>

<p>
    <a asp-action="Create" class="btn btn-success" >Añadir reseñas </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Calificación)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comentario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaReseña)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Producto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Calificación)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comentario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaReseña)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.Nombre)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cliente.Apellido)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.Nombre)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-primary btn-sm" asp-route-id="@item.ReseñaId">Editar</a> |
                    <a asp-action="Details" class="btn btn-warning" asp-route-id="@item.ReseñaId">Detalles</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.ReseñaId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>


@section scripts
{
    <script>
        $(document).ready(function () {
            new DataTable('#Tabla');
        });
    </script>
}

